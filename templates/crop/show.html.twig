{% extends 'base.html.twig' %}

{% block title %}Crop{% endblock %}

{% block body %}
    <h1>Crop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crop.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crop_index') }}">back to list</a>

    <a href="{{ path('crop_edit', {'id': crop.id}) }}">edit</a>

    {{ include('crop/_delete_form.html.twig') }}
{% endblock %}
