{% extends 'base.html.twig' %}

{% block title %}FieldProcessing{% endblock %}

{% block body %}
    <h1>FieldProcessing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ field_processing.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ field_processing.date ? field_processing.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Area</th>
                <td>{{ field_processing.area }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('field_processing_index') }}">back to list</a>

    <a href="{{ path('field_processing_edit', {'id': field_processing.id}) }}">edit</a>

    {{ include('field_processing/_delete_form.html.twig') }}
{% endblock %}
