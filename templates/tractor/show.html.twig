{% extends 'base.html.twig' %}

{% block title %}Tractor{% endblock %}

{% block body %}
    <h1>Tractor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tractor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tractor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tractor_index') }}">back to list</a>

    <a href="{{ path('tractor_edit', {'id': tractor.id}) }}">edit</a>

    {{ include('tractor/_delete_form.html.twig') }}
{% endblock %}
